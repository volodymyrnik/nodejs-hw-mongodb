type: object
required:
  - email
  - password
properties:
  email:
    type: string
    description: Contact's email
    format: email
    example: example@example.com
  password:
    type: string
    description: Contact's password
    format: password
    example: 1234567